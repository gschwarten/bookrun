services:
  - type: web
    name: bookrun
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --threads 8
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: GOODREADS_USER_ID
        sync: false
